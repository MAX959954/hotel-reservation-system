# 8081, not 8080 — docker-compose maps the backend container's 8080 to host port 8081.
# Only use 8080 here if you're running the backend directly via ./gradlew bootRun.
VITE_API_BASE_URL=http://localhost:8081
VITE_GOOGLE_CLIENT_ID=
VITE_STRIPE_PUBLISHABLE_KEY=
